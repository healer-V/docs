## 一、介绍vue3
::: tip Vue3
 - 2020年10月发布
 - 一套用于构建用户界面的`渐进式框架`
 - 可以自底向上逐层应用。
 - Vue 的核心库只关注视图层。
 - 基于`TypeScript`开发
:::
### 1.1、MVVM架构

1. `View`：视图层（UI 用户界面）
2. `ViewModel`：业务逻辑层（一切 js 可视为业务逻辑）
3. `Model`：数据层（存储数据及对数据的处理如增删改查）

### 1.2、vue2 对比 vue3
> [!IMPORTANT] 区别
>- 支持`Fragments`多根元素
>- 支持`Composition API`
>- 全局API改进
>- 自定义渲染器
>- 支持`TypeScript`
>- Teleport
>- Suspense
>- 支持Tree shaking
>- 重写了双向数据绑定
>- 性能优化：静态树提升、Proxy响应式核心

### 1.3、Vue3新特性
#### 1.3.1、重写双向数据绑定
>[!TIP]
>- `Vue2`基于`Object.defineProperty()`实现
>
>- `Vue3` 基于`Proxy`实现
>
> `proxy`与`Object.defineProperty(obj, prop, desc)`方式相比有以下`优势`：
>
>  1. 丢掉麻烦的备份数据
>  2. 省去for in 循环
>  3. 可以监听数组变化
>  4. 代码更简化
>  5. 可以监听动态新增的属性；
>  6. 可以监听删除的属性；
>  7. 可以监听数组的索引和 length 属性；

```js
 
    let proxyObj = new Proxy(obj,{
        get : function (target,prop) {
            return prop in target ? target[prop] : 0
        },
        set : function (target,prop,value) {
            target[prop] = 888;
        }
    })
```



#### 1.3.2、Vue3优化VDOM
>[!TIP]
> - 在Vue2中,每次更新diff,都是全量对比。
> - Vue3则只对比`带有标记的`,这样大大减少了非动态内容的对比消耗
> 
>
> 『注』：可以通过[Vue Template Explorer](https://vue-next-template-explorer.netlify.app/) 网站看到静态标记	

#### 1.3.3、Vue3 Fragments
>[!TIP]
> - Vue3 支持`多个根节点`
> - 支持`render JSX `写法
> - 新增了`<Suspense>` 内置组件：在组件树中协调对异步依赖的处理。
>   - 可以在组件树上层等待下层的多个嵌套异步依赖项解析完成，并可以在等待时渲染一个加载状态。
> - 新增了`<teleport>` 内置组件：
>   - 将一个组件内部的一部分模板“传送”到该组件的 DOM 结构外层的位置去。
> - 新增了多 `v-model` 用法

```js
render() {
        return (
            <>
                {this.visable ? (
                    <div>{this.obj.name}</div>
                ) : (
                    <div>{this.obj.price}</div>
                )}
                <input v-model={this.val}></input>
                {[1, 2, 3].map((v) => {
                   return <div>{v}-----</div>;
                })}
            </>
        );
    },
 
```

#### 1.3.4、Vue3 Tree shaking
>[!TIP]
> - 在保持代码运行结果不变的前提下，`去除无用的代码`。
> - `Vue2中不支持`，主要原因是Vue实例在项目中是单例的，捆绑程序无法检测到该对象的哪些属性在代码中被使用到。
> - Vue3源码引入tree shaking特性，`将全局 API 进行分块`。

#### 1.3.5、Vue 3 Composition Api
>[!NOTE]
> - `Vue3` 引入了`Composition API`，它是基于`组合式 API`的思想，提供了一系列函数式的方法来帮助我们更好地组织代码。


::: details 点击查看
1. **性能优化：**
   - Vue.js 3引入了虚拟DOM的重写，提供了更快的渲染性能。这意味着Vue.js 3在处理大型组件树时更高效，减少了内存使用和渲染时间。
   - Vue.js 3使用了Proxy来实现响应式数据，相比Vue.js 2的Object.defineProperty，它具有更好的性能，支持深层嵌套对象的响应式。
   - 静态树提升（Static Tree Hoisting）是Vue.js 3的新特性，可以进一步提高渲染性能，特别是对于包含大量静态内容的组件。

2. **Composition API：**
   - Vue.js 3引入了Composition API，它是一种更灵活和可组合的API风格，允许开发者更好地组织和重用逻辑代码，而不受限于Options API的约束。Composition API在大型项目中尤其有用。

3. **全局 API 改进：**
   - 在Vue.js 3中，全局API（如`Vue.directive`、`Vue.component`等）已经被移动到单独的模块中，以提高代码的可维护性。这意味着你需要在组件中显式导入这些API，而不是在全局范围内自动注册它们。

4. **Teleport：**
   - Vue.js 3引入了Teleport组件，它允许你将组件的内容渲染到DOM中的不同位置，这在处理模态框、弹出菜单等情况下非常有用。

5. **Suspense：**
   - Vue.js 3引入了Suspense特性，它允许你在异步加载组件时显示占位符，以改善用户体验。这在处理懒加载路由、异步组件等方面非常有用。

6. **多根元素支持：**
   - 在Vue.js 3中，组件可以返回多个根元素，而不像Vue.js 2那样只能有一个根元素。这简化了组件的编写和布局。

7. **Tree Shaking：**
   - Vue.js 3的代码构建更容易进行Tree Shaking，这意味着你可以更轻松地剔除不使用的代码，减小项目的包体积。

8. **自定义渲染器：**
   - Vue.js 3引入了更强大的自定义渲染器API，允许你在不同平台上（如Web、移动、桌面、原生应用等）使用Vue。

9. **TypeScript支持：**
   - Vue.js 3对TypeScript的支持更加友好，内置了更多的类型声明，使得在TypeScript项目中使用Vue更加容易。
:::



## 三、vite目录

### 3.1、目录介绍
::: tip Vite
 - `public` :下面的不会被编译 可以存放静态资源。
 - `assets` :下面可以存放可编译的静态资源。
 - `components`: 下面用来存放我们的组件。
 - `App.vue` :全局组件。
 - `main.ts` :全局的ts文件。
 - `index.html ` : 非常重要的入口文件 。
 - webpack，rollup 他们的入口文件都是enrty input 是一个js文件 。
- 而Vite 的入口文件是一个html文件，他刚开始不会编译这些js文件 只有当你用到的时候 如script src="xxxxx.js" 会发起一个请求被vite拦截这时候才会解析js文件。
  - `vite.config.ts` 是vite的配置文件具体配置项。
 - VsCode Vue3 插件推荐 Vue Language Features (Volar)。
 :::



## 四、Vue3 生命周期
>[!danger]
> Vue3 生命周期分为三个阶段：
> - `setup`：在组件实例被创建之前，被调用，返回数据和方法。
> - `onBeforeMount`：在挂载开始之前被调用
> - `onMounted`：在组件被挂载之后被调用
> - `onBeforeUpdate`：在数据更新之前被调用
> - `onUpdated`：在数据更新之后被调用
> - `onactivated`：在组件被激活时被调用
> - `ondeactivated`：在组件被销毁时被调用
> - `onBeforeUnmount`：在组件销毁之前被调用
> - `onUnmounted`：在组件销毁之后被调用
## 五、模板语法 & vue指令

### 5.1、模板插值语法
>[!TIP] 语法
> 在script 声明一个变量可以直接在template 使用用法为{{变量名称}}
> - 可以编写条件运算
> - 简单运算
> - 操作API 
::: details 点击查看示例
```vue
<template>
  <div>{{ message }}</div>
 	<div>{{ mess == 0 ? '我是张三0' : '我不是张三other' }}</div>
  <div>{{ mess  + 1 }}</div>
  <div>{{ message.split('，') }}</div>
</template>
 
 
<script setup lang="ts">
const message = "我是张三"
const mess:number = 1
</script>
<style>
</style>
```
:::

### 5.2、Vue指令
>[!TIP] 指令
> 1. `v-text `: 用来显示文本
> 2. `v-html`:  用来展示富文本
> 3. `v-if`: 用来控制元素的显示隐藏（切换真假DOM）
> 4. `v-else v-if`: 条件收尾语句
> 5. `v-show`: 用来控制元素的显示隐藏（display none block Css切换）
> 6. `v-on`: 简写`@ `用来给元素添加事件
> 7. `v-bind`: 简写`: ` 用来绑定元素的属性Attr
> 8. `v-model`: 双向绑定
> 9. `v-for`: 用来遍历元素
> 10. `v-once `:性能优化只渲染一次
> 11. `v-memo`:小幅度手动提升一部分`性能`
> 12. `v-prevent`阻止表单提交
> 13. `v-cloak`: 隐藏未编译的元素
> 14. `v-pre`: 跳过这个元素和它的子元素的编译过程，直接显示原始的 Mustache 语法。
> 15. `v-is`: 用于动态组件，动态选择要渲染的组件 



## 六、Vue核心虚拟Dom和 diff 算法
>[!tip]
> `虚拟DOM`就是通过JS来生成一个AST(抽象语法树)节点树

### 6.1、虚拟Dom（VNode）

假设真实DOM是：

```html
<ul id="container">
    <li class="box" :key="user1">张三</li>
    <li class="box" :key="user2">李四</li>
</ul>
```

那么它对应的VNode就是：
::: details 点击查看虚拟DOM
```js
<script>
let oldVNode = {
  tag: "ul",
  data: {
    staticClass: "container",
  },
  text: undefined,
  children: [
    {
      tag: "li",
      data: { staticClass: "box", key: "user1" },
      text: undefined,
      children: [
        { tag: undefined, data: undefined, text: "张三", children: undefined },
      ],
    },
    {
      tag: "li",
      data: { staticClass: "box", key: "user2" },
      text: undefined,
      children: [
        { tag: undefined, data: undefined, text: "李四", children: undefined },
      ],
    },
  ],
};
</script>
```
:::


### 6.2、Vue2 Diff算法
>[!tip]
> 整体策略：`深度优先，同层比较`。
>
> 关键特性：
>1. `同级比较`: 只比较同一层级的节点。
>2. `双端比较`：采用双端比较策略，从列表的两端（头部和尾部）开始比较，以尽量减少节点的移动次数。
>3. `更新策略`：当头尾比较无法匹配时，Vue2 会尝试复用旧节点，通过更新节点的属性或子节点来匹配新的虚拟节点，同时将其移动到正确的位置，以减少 DOM 操作次数。

#### 6.2.1、Vue2 Diff基本原理
>[!tip]
> 1. 首先进行新旧节点头尾对比，头与头，尾与尾对比，寻找为移动的节点。
> 2. 新旧节点头尾对比完成后，进行交叉对比，头与尾，尾与头对比，这一步是寻找移动后可复用的节点。
> 3. 在剩余的新旧节点中对比寻找可复用节点，创建一个旧节点keyToIndex的哈希表map记录key，然后继续遍历新节点索引通过key查找可复用的旧节点。

#### 6.2.2、算法实现

### 6.3、Vue3 Diff算法

#### 6.3.1、Vue3 Diff基本原理
>[!tip]
>  **全新的编译策略和运行时优化，包括对 Diff 算法的改进**
>
>  1. `双端比较优化`:对于相同节点的处理更加高效。
>  2. `静态节点提升`:对静态节点进行提升，这些节点在更新时不会被重新创建，而是直接复用，减少渲染成本。
>  3. `支持碎片化(Fragment)`:Vue3支持碎片化，允许组件有多个根节点，这在Vue2中是不支持的。
>  4. `区块树(Block Tree)`:可以跳过静态内容，快速定位到动态节点，减少了Diff时的比较次数。
>  5. `编译时优化`：Vue3在编译时会对模板进行静态提升，将不会变化的节点和属性提取出来，避免在每次渲染时都重新创建，以此减少DOM树的创建和销毁。


#### 6.3.2、Vue3 无key的Diff算法
>[!tip]
> 1. patch的时候不同节点会进行替换。
> 2. 多余的节点进行新增。
> 3. 缺少了原节点会进行删除。



#### 6.3.3、Vue3 有key的Diff算法
>[!tip]
> 1. 前序对比算法。
> 2. 尾序对比算法。
> 3. 新节点如果多出来，就是挂载。
> 4. 旧节点如果多出来，就是卸载。
> 5. 特殊情况乱序。
>
>    1. 构建新节点的映射关系。
>    2. 记录新节点在旧节点中的位置数组。
>    3. 多余旧节点就进行删除。
>    4. 新节点不包含的旧节点也删除。
>    5. 节点出现交叉，说明是要移动，去求最长递增子序列。
>       - 求最长递增子序列升序（贪心+二分查找）。
>    6. 如果当前遍历的这个节点不在子序列，说明要进行移动，如果节点在子序列中直接跳过。


### 6.4、vue2与vue3 diff算法的区别

> [!TIP]
> - Vue 2 使用的是基于`递归的双指针`的 diff 算法， Vue 3 使用的是基于`数组的动态规划`的 diff 算法,Vue 3 的算法效率更高，因为它使用了一些优化技巧，例如按需更新、静态标记等。
>- Vue 2 的 diff 算法会对整个组件树进行完整的遍历和比较，而 Vue 3 的 diff 算法会跳过静态子树的比较，只对动态节点进行更新。这减少了不必要的比较操作，提高了性能。
>- Vue 2 的 diff 算法对于列表渲染（v-for）时的元素重新排序会比较低效，需要通过给每个元素设置唯一的 key 来提高性能。而 Vue 3 的 diff 算法在列表渲染时，通过跟踪元素的移动，可以更好地处理元素的重新排序，无需设置 key。
>- Vue 3 的 diff 算法对于静态节点的处理更加高效，静态节点只会在首次渲染时被处理，后续更新时会直接跳过比较和更新操作，减少了不必要的计算。

> 1. Vue3 的 Diff 算法相比 Vue2 在性能上有明显的提升。
> 2. 对静态节点和动态节点的处理更加高效。
> 3. Vue3 的`区块树优化`也减少了不必要的节点比较，进一步提高了性能。

#### 6.4.1、Vue2 diff算法具体实现
> [!TIP]
> - Vue2 使用的是基于`递归的双指针`的 diff 算法，其核心思想是：
>   - 首先，创建一个`vnode`树，`vnode`树是由组件的`template`编译生成的，`vnode`树的每个节点都包含了`tag`、`data`、`children`等信息。
>   - 然后，对`vnode`树进行`diff`操作，`diff`操作会递归地比较新旧`vnode`树的每个节点，并生成一个`patch`对象，描述了如何将旧`vnode`树转变成新`vnode`树。
>   - 最后，将`patch`对象应用到真实的`DOM`上，完成视图更新。
> - Vue2 的`diff`算法的实现比较简单，但是对于复杂的场景，比如`列表渲染`、`动态组件`等，`diff`算法的效率会比较低。
::: details 点击查看Vue2 diff算法实现
```js
function patchVnode(oldVnode, newVnode) {
    if (oldVnode === newVnode) {
        return;
    }
    const { tag, data, children } = newVnode;
    if (oldVnode.tag !== tag) {
        const el = document.createElement(tag);
        patch(oldVnode, el);
        parent.replaceChild(el, oldVnode.el);
    } else {
        const el = oldVnode.el;
        const oldData = oldVnode.data;
        const newData = data || {};
        if (oldData.class !== newData.class) {
            el.className = newData.class;
        }
        if (oldData.style !== newData.style) {
            el.style.cssText = newData.style;
        }
        if (children) {
            if (Array.isArray(children)) {
                updateChildren(el, children, oldVnode.children);
            } else {
                el.textContent = children;
            }
        }
    }
}

function updateChildren(parent, newChildren, oldChildren) {
    let oldStart = 0;
    let newStart = 0;
    let oldEnd = oldChildren.length - 1;
    let newEnd = newChildren.length - 1;
    let oldStartVnode = oldChildren[0];
    let newStartVnode = newChildren[0];
    let oldEndVnode = oldChildren[oldEnd];
    let newEndVnode = newChildren[newEnd];
    while (oldStart <= oldEnd && newStart <= newEnd) {
        if (!oldStartVnode) {
            oldStartVnode = oldChildren[++oldStart];
        } else if (!oldEndVnode) {
            oldEndVnode = oldChildren[--oldEnd];
        } else if (sameVnode(oldStartVnode, newStartVnode)) {
            patchVnode(oldStartVnode, newStartVnode);
            oldStartVnode = oldChildren[++oldStart];
            newStartVnode = newChildren[++newStart];
        } else if (sameVnode(oldEndVnode, newEndVnode)) {
            patchVnode(oldEndVnode, newEndVnode);
            oldEndVnode = oldChildren[--oldEnd];
            newEndVnode = newChildren[--newEnd];
        } else if (sameVnode(oldStartVnode, newEndVnode)) {
            patchVnode(oldStartVnode, newEndVnode);
            parent.insertBefore(oldStartVnode.el, oldEndVnode.el.nextSibling);
            oldStartVnode = oldChildren[++oldStart];
            newEndVnode = newChildren[--newEnd];
        } else if (sameVnode(oldEndVnode, newStartVnode)) {
            patchVnode(oldEndVnode, newStartVnode);
            parent.insertBefore(oldEndVnode.el, oldStartVnode.el);
            oldEndVnode = oldChildren[--oldEnd];
            newStartVnode = newChildren[++newStart];
        } else {
            if (oldStartVnode.tag === "option") {
                patchVnode(oldStartVnode, newStartVnode);
                newStartVnode = newChildren[++newStart];
            } else {
                const oldKey = oldStartVnode.key;
                const newKey = newStartVnode.key;
                if (!newKey) {
                    patchVnode(oldStartVnode, newStartVnode);
                    newStartVnode = newChildren[++newStart];
                } else if (!oldKey) {
                    patchVnode(oldStartVnode, newStartVnode);
                    oldStartVnode = oldChildren[++oldStart];
                } else if (oldKey === newKey) {
                    patchVnode(oldStartVnode, newStartVnode);
                    oldStartVnode = oldChildren[++oldStart];
                    newStartVnode = newChildren[++newStart];
                } else {
                    const index = newChildren.findIndex((vnode) => vnode.key === newKey);
                    if (index > -1) {
                        patchVnode(oldStartVnode, newChildren[index]);
                        newStartVnode = newChildren[++newStart];
                    } else {
                        patchVnode(oldStartVnode, newStartVnode);
                        oldStartVnode = oldChildren[++oldStart];
                    }
                }
            }
        }
    }
    if (oldStart <= oldEnd || newStart <= newEnd) {
        if (oldStart > oldEnd) {
            const refNode = newChildren[newEnd + 1] ? newChildren[newEnd + 1].el : null;
            addVnodes(parent, refNode, newChildren, newStart, newEnd);
        } else {
            removeVnodes(parent, oldChildren, oldStart, oldEnd);
        }
    }
}
```
:::



#### 6.4.2、Vue3 diff算法具体实现
> [!TIP]
> - Vue3 使用的是基于`数组的动态规划`的 diff 算法，其核心思想是：
>   - 首先，创建一个`vnode`数组，`vnode`数组是由组件的`template`编译生成的，`vnode`数组的每个节点都包含了`tag`、`data`、`children`等信息。
>   - 然后，对`vnode`数组进行`diff`操作，`diff`操作会生成一个`patch`数组，描述了如何将旧`vnode`数组转变成新`vnode`数组。
>   - 最后，将`patch`数组应用到真实的`DOM`上，完成视图更新。
> - Vue3 的`diff`算法的实现比较复杂，但是对于复杂的场景，比如`列表渲染`、`动态组件`等，`diff`算法的效率会比较高。
::: details 点击查看Vue3 diff算法实现
```js
function patch(oldVnode, newVnode) {
    const el = (oldVnode && oldVnode.el) || document.createElement(newVnode.tag);
    const parent = el.parentNode;
    if (oldVnode) {
        update(oldVnode, newVnode);
    } else {
        create(newVnode);
    }
    parent.insertBefore(el, el.nextSibling);
}

function update(oldVnode, newVnode) {
    if (oldVnode.tag !== newVnode.tag) {
        const el = oldVnode.el;
        const parent = el.parentNode;
        parent.removeChild(el);
        create(newVnode);
        parent.insertBefore(el, el.nextSibling);
    } else {
        updateChildren(oldVnode.el, oldVnode.children, newVnode.children);
        updateData(oldVnode.el, oldVnode.data, newVnode.data);
    }
}

function updateChildren(parent, oldChildren, newChildren) {
    const oldStartIndex = 0;
    const newStartIndex = 0;
    const oldEndIndex = oldChildren.length - 1;
    const newEndIndex = newChildren.length - 1;
    let oldStartVnode = oldChildren[oldStartIndex];
    let newStartVnode = newChildren[newStartIndex];
    let oldEndVnode = oldChildren[oldEndIndex];
    let newEndVnode = newChildren[newEndIndex];
    let oldKeyToIndexMap = {};
    let newKeyToIndexMap = {};
    let i, j;
    for (i = oldStartIndex; i <= oldEndIndex; i++) {
        oldKeyToIndexMap[oldChildren[i].key] = i;
    }
    for (j = newStartIndex; j <= newEndIndex; j++) {
        newKeyToIndexMap[newChildren[j].key] = j;
    }
    while (oldStartIndex <= oldEndIndex && newStartIndex <= newEndIndex) {
        if (!oldStartVnode) {
            oldStartVnode = oldChildren[++oldStartIndex];
        } else if (!oldEndVnode) {
            oldEndVnode = oldChildren[--oldEndIndex];
        } else if (sameVnode(oldStartVnode, newStartVnode)) {
            patch(oldStartVnode, newStartVnode);
            oldStartVnode = oldChildren[++oldStartIndex];
            newStartVnode = newChildren[++newStartIndex];
        } else if (sameVnode(oldEndVnode, newEndVnode)) {
            patch(oldEndVnode, newEndVnode);
            oldEndVnode = oldChildren[--oldEndIndex];
            newEndVnode = newChildren[--newEndIndex];
        } else if (sameVnode(oldStartVnode, newEndVnode)) {
            patch(oldStartVnode, newEndVnode);
            parent.insertBefore(oldStartVnode.el, oldEndVnode.el.nextSibling);
            oldStartVnode = oldChildren[++oldStartIndex];
            newEndVnode = newChildren[--newEndIndex];
        } else if (sameVnode(oldEndVnode, newStartVnode)) {
            patch(oldEndVnode, newStartVnode);
            parent.insertBefore(oldEndVnode.el, oldStartVnode.el);
            oldEndVnode = oldChildren[--oldEndIndex];
            newStartVnode = newChildren[++newStartIndex];
        } else {
            if (oldStartVnode.key === newEndVnode.key) {
                patch(oldStartVnode, newEndVnode);
                parent.insertBefore(oldStartVnode.el, oldEndVnode.el.nextSibling);
                oldStartVnode = oldChildren[++oldStartIndex];
                newEndVnode = newChildren[--newEndIndex];
            } else if (oldEndVnode.key === newStartVnode.key) {
                patch(oldEndVnode, newStartVnode);
                parent.insertBefore(oldEndVnode.el, oldStartVnode.el);
                oldEndVnode = oldChildren[--oldEndIndex];
                newStartVnode = newChildren[++newStartIndex];
            } else {
                if (oldStartVnode.key && newEndVnode.key) {
                    if (oldStartVnode.key === newEndVnode.key) {
                        patch(oldStartVnode, newEndVnode);
                        parent.insertBefore(oldStartVnode.el, oldEndVnode.el.nextSibling);
                        oldStartVnode = oldChildren[++oldStartIndex];
                        newEndVnode = newChildren[--newEndIndex];
                    } else {
                        const oldIndex = oldKeyToIndexMap[newStartVnode.key];
                        if (oldIndex !== undefined) {
                            patch(oldChildren[oldIndex], newStartVnode);
                            parent.insertBefore(oldChildren[oldIndex].el, oldStartVnode.el);
                            newStartVnode = newChildren[++newStartIndex];
                        } else {
                            patch(oldStartVnode, newStartVnode);
                            newStartVnode = newChildren[++newStartIndex];
                        }
                    }
                } else if (oldEndVnode.key && newStartVnode.key) {
                    if (oldEndVnode.key === newStartVnode.key) {
                        patch(oldEndVnode, newStartVnode);
                        parent.insertBefore(oldEndVnode.el, oldStartVnode.el);
                        oldEndVnode = oldChildren[--oldEndIndex];
                        newStartVnode = newChildren[++newStartIndex];
                    } else {
                        const oldIndex = oldKeyToIndexMap[newEndVnode.key];
                        if (oldIndex !== undefined) {
                            patch(oldChildren[oldIndex], newEndVnode);
                            parent.insertBefore(oldChildren[oldIndex].el, oldEndVnode.el.nextSibling);
                            newEndVnode = newChildren[--newEndIndex];
                        } else {
                            patch(oldEndVnode, newEndVnode);
                            newEndVnode = newChildren[--newEndIndex];
                        }
                    }
                } else {
                    patch(oldStartVnode, newStartVnode);
                    newStartVnode = newChildren[++newStartIndex];
                }
            }
        }
    }
    if (oldStartIndex <= oldEndIndex || newStartIndex <= newEndIndex) {
        if (oldStartIndex > oldEndIndex) {
            const refNode = newChildren[newEndIndex + 1] ? newChildren[newEndIndex + 1].el : null;
            addVnodes(parent, refNode, newChildren, newStartIndex, newEndIndex);
        } else {
            removeVnodes(parent, oldChildren, oldStartIndex, oldEndIndex);
        }
    }
}

function addVnodes(parent, refNode, vnodes, startIdx, endIdx) {
    for (; startIdx <= endIdx; ++startIdx) {
        const vnode = vnodes[startIdx];
        parent.insertBefore(create(vnode), refNode);
    }
}

function removeVnodes(parent, vnodes, startIdx, endIdx) {
    for (; startIdx <= endIdx; ++startIdx) {
        const vnode = vnodes[startIdx];
        if (vnode.el) {
            parent.removeChild(vnode.el);
        }
    }
}

function create(vnode) {
    const el = document.createElement(vnode.tag);
    updateData(el, {}, vnode.data);
    if (vnode.children) {
        if (Array.isArray(vnode.children)) {
            addVnodes(el, null, vnode.children, 0, vnode.children.length - 1);
        } else {
            el.textContent = vnode.children;
        }
    }
    return el;
}

function updateData(el, oldData, newData) {
    for (const key in newData) {
        const value = newData[key];
        if (key === "class") {
            el.className = value;
        } else if (key === "style") {
            el.style.cssText = value;
        } else if (key[0] === "o" && key[1] === "n") {
            const eventName = key.slice(2).toLowerCase();
            if (oldData[key] !== value) {
                el.removeEventListener(eventName, oldData[key]);
                el.addEventListener(eventName, value);
            }
        } else if (key !== "key") {
            el.setAttribute(key, value);
        }
    }
    for (const key in oldData) {
        if (newData[key] === undefined) {
            el.removeAttribute(key);
        }
    }
} 

function sameVnode(oldVnode, newVnode) {
    return (
        oldVnode.tag === newVnode.tag &&
        oldVnode.key === newVnode.key &&
        oldVnode.isComment === newVnode.isComment
    );
}
```
:::

### 6.5、算法相关

#### 6.5.1、最长递增子序列

> Longest Increasing Subsequence，简称LIS ：要求在一个给定的序列中找到最长的严格递增的子序列。
>
> 方法：
>
> 1. 动态规划
> 2. 贪心+二分查找



```js
function LIS(nums) {
  if (nums.length === 0) {
    return [];
  }
  let results = [[nums[0]]];
  for (let i = 1; i < nums.length; i++) {
    const n = nums[i];
    _update(n);
  }
  function _update(n) {
    for (let i = results.length - 1; i >= 0; i--) {
      const line = results[i];
      const tail = line[line.length - 1];
      if (n > tail) {
        results[i + 1] = [...line, n];
        break;
      } else if (n < tail && i === 0) {
        results[i] = [n];
      }
    }
  }
  return results[results.length - 1];
}
```










#### 6.5.2、前序遍历

> 根左右



#### 6.5.3、中序遍历

> 左根右



#### 6.5.4、后序遍历
> 左右根



## 七、Ref全家桶

### 7.1、ref

#### 7.1.1、定义
>[!tip]
> 接受一个内部值并返回一个`响应式且可变`的 ref 对象。ref 对象仅有一个 `.value` property，指向该内部值。

#### 7.1.2、用法

```js
const test1 = ref({name:"张三"})
```



### 7.2、isRef

#### 7.2.1、定义
>[!tip]
> 判断变量是否为响应式



#### 7.2.2、用法

```js
const test1 = ref({name:"张三"})
console.log(isRef(test1)) // true
```

### 7.3、shallowRef

#### 7.3.1、定义
>[!tip]
> 浅层次的响应
> 1. `shallowRef`是`ref`的浅层作用形式。
> 2. 只有对 `.value` 的访问是响应式的。

>[!tip]
> 注意：`shallowRef`不能与`Ref`同时使用，不然`会影响shallowRef造成视图更新`，因为Ref会调用`triggerRef`导致依赖收集，触发依赖更新。
::: details 点击查看示例
```js
<template>
		<div>{{test2}}</div>    
</template>
<script setup lang="ts">
const test2 = shallowRef({name:"张三"})
const touch =() =>{
    test2.value.name ="李四" // 视图不更新
}

</script>
```

```js
<template>
   <div>{{test1}}</div> 
		<div>{{test2}}</div>    
</template>
<script setup lang="ts">
const test1 = shallowRef({name:"王五"})
const test2 = shallowRef({name:"张三"})
const touch =() =>{
    test1.value.name ="王五123" 
    test2.value.name ="李四" // 视图更新
}

</script>
```
:::

### 7.4、triggerRef

#### 7.4.1、定义
>[!tip]
> **强制触发响应，手动更新视图**

#### 7.4.2、用法
>[!tip]
> 接收一个参数 state ，即需要更新的 ref 对象。
> - `triggerRef(state)`
::: details 点击查看示例
```js
<template>
  <div>
    <p>姓名：{{info.name}}</p>
    <p>年龄：{{info.age}}岁</p>

    <button @click="modifyInfo">点击按钮修改信息</button> 
  </div> 
</template>

<script setup>
import { shallowRef, triggerRef } from 'vue'
    
    const info = shallowRef({
      name: '张三',
      age: 20,
      
    })

    const modifyInfo = () => {
        info.value.name = '李四'
        info.value.age = 23 
        triggerRef(info)
    }  
</script>

```
:::

### 7.5、customRef

#### 7.5.1、定义
>[!tip]
> 创建一个`自定义的 ref`，显式声明对其依赖追踪和更新触发的控制方式。

#### 7.5.2、用法
::: details 点击查看示例
```js
import { customRef } from 'vue';

// customRef用于自定义Ref
function MyRef(value) {
    // 该函数默认带参数 track 和 trigger 两个方法。
    return customRef((track, trigger) => {
   
      return {
        // 该对象需要包含 get 和 set 方法。
        get() {
          // 追踪数据变化
          track() // 收集依赖
          return value
        },
        // set 传入一个值作为新值，通常用于取代value
        set(newValue) {
          value = newValue
          // 触发响应，更新视图
          trigger() // 触发依赖更新
        }
      }
    })
}

```
:::


## 八、Reactive全家桶

### 8.1、reactive

#### 8.1.1、定义

> 接受一个内部值并返回一个`响应式且可变`的 reactive对象。

#### 8.1.2、用法

```js
let form = reactive({
    name:"张三"
})
```



#### 8.1.3、与ref的区别

> 1. `ref`支持所有的类型，reactive只支持引用类型（`Array`、`Object`、`Map`、`Set`）
> 2. `ref`取值时都需要加`.value`获取，reactive不需要。

#### 8.1.4、注意点
>[!tip]
> `rective`不能直接赋值整个对象的值，不然会对Proxy代理进行`覆盖`，影响视图更新。
>
解决方案：
1. 数组可以使用push加解构
::: details 点击查看示例
 ````js
 let list = reactive([])
 const add =()=>{
     setTimeout(()=>{
         let res = ['EDG','FPX','IG']
         list.push(res)
         console.log(list)
     },1000)
 }
 ````
:::
 2. 对象赋值其属性
 ::: details 点击查看示例
 ```js
 let list = reactive({
     arr:[]
 })
 const add =()=>{
     setTimeout(()=>{
         let res = ['EDG','FPX','IG']
         list.arr= res
         console.log(list)
     },1000)
 }
 ```
:::

### 8.2、shallowReactive

#### 8.2.1、定义
>[!tip]
> 浅层次的响应
> 1. `shallowRective`是`rective`的浅层作用形式。
> 2. 响应式只到其变量的第一层属性，如let list = reactive({index:{num:{foo:1}}}) ,只到list.index有响应式。

#### 8.2.2、用法
::: details 点击查看示例
```js
let list = shallowReactive({
    foo:{
        num:{
            bar:1
        }
    }
})

```
:::


## 九、to系列

### 9.1、toRef

#### 9.1.1、定义
>[!tip]
> 只能修改响应式的值，非响应式对象视图不会更新

#### 9.1.2、用法
::: details 点击查看示例
```js
<div>{{like}}</div>

import{toRef,reactive} from 'vue'
// const man = {name:'张三',age:22,hooby:'football'}
const man = reactive({name:'张三',age:22,hooby:'football'})
const hooby = toRef(man,"hooby")

const change = ()=>{
    hooby.value = 'basketball'
}
```
:::
### 9.2、toRefs

#### 9.2.1、定义
>[!tip]
> 只能修改响应式的值，非响应式对象视图不会更新

#### 9.2.2、用法



## 十、响应式原理

### 10.1、Vue2响应式原理
>[!tip]
> - `Vue2`使用的是`Object.defineProperty`
>   - `Object.defineProperty`只能劫持对象的属性，不能劫持数组的索引。
>   - 原理：获取数据时触发`getter`属性收集依赖，修改数据时触发`setter`属性更新依赖。
> - `Vue3`使用的是`Proxy`
>   - `Proxy`可以代理整个对象，拦截对象的各种操作。
>   - 原理：
>     1. 依赖收集: 在属性访问时，会进行依赖收集。通过track函数将当前的副作用函数（如组件的渲染函数、计算属性的求值函数等）与被访问的属性建立关联，将副作用函数存储在一个依赖集合中。
>     2. 依赖更新: 当数据发生变化时，会触发依赖更新。通过trigger函数遍历依赖集合，调用副作用函数，使得视图更新。

#### 10.1.1、Vue2.0不足

> 1. 对象只能劫持设置好的数据，新增的数据需要使用`$set`。
> 2. 数组只能操作七种方法，修改某一项值无法劫持。
> `Object.defineProperty`是能做到对数组的修改，只是有非常大的性能问题，所以只能做性能与操作之间的平衡。


### 10.2、实现reactive

::: details 点击查看代码
```js
export const reactive = <T extends object>(target:T)=>{
    return new Proxy(target,{
        get(target,key,receiver){
            let res = Reflect.get(target,key,receiver) // 使用Reflect是为了防止上下文错乱
            return res
        },
        set(target,key,value,receiver){
            let res = Reflect.get(target,key,value,receiver)
            return res
        }
    })
}
```
:::

### 10.3、实现副作用函数
> 副作用函数是指当数据发生变化时，需要执行的函数。

## 十一、Computed计算属性

### 11.1、定义
>[!tip]
> 当依赖的属性的值发生改变时，才会触发它的更改，如果依赖的值不改变，使用的是缓存中的属性值。
### 11.2、底层原理
>[!note]
> 1. 依赖收集： 当 Vue 实例初始化时，会对 Computed 计算属性中涉及的响应式数据进行依赖收集，在访问计算属性时，会触发get函数，在这个函数内部会调用track函数来收集依赖。
> 2. 建立依赖关系： 每个计算属性都有一个对应的Watcher实例，它会在收集依赖的过程中，将自身与所依赖的响应式数据建立关联。
> 3 缓存机制： 
>    1. `计算属性的缓存`：计算属性会缓存其计算结果，只有在其依赖的响应式数据发生变化时，才会重新计算。在计算属性的Watcher实例中，有一个标志位用于记录计算属性是否已经被计算过，以及一个变量用于存储计算结果。
>    2. `判断是否需要重新计算`：当依赖的响应式数据发生变化时，会触发set函数，进而触发trigger函数通知相关的Watcher实例进行更新。Watcher实例在收到更新通知后，会根据缓存标志和依赖数据的变化情况，判断是否需要重新计算。如果计算属性依赖的数据没有变化，就直接返回缓存中的结果，而不会重新执行计算函数。


### 11.2、选项式写法
>[!note]
> 支持一个对象传入get函数以及set函数自定义操作
::: details 点击查看代码
```js
<template>
    <div>姓：<input v-model="firstname" type="text"></div>
    <div>名：<input v-model="lastname" type="text"></div>
    
    <div>全名：{{name}}</div>
			<div><button @click="changeName">changeName</button></div>
</template>


<script setup lang="ts">
let firstname = ref("张")
let lastname = ref("三")
let name =computed<string>({
    get(){
        return firstname.value + '-' + lastname.value
    },
    set(newVal){
        [firstname.value,lastname.value] = newVal.split('-')
    }
})

const changName = ()=>{
    name.value = '李-四'
}
```
:::

### 11.2、函数式写法
>[!tip]
> 只能支持一个getter函数不允许修改值
::: details 点击查看代码
```js
<template>
    <div>姓：<input v-model="firstname" type="text"></div>
    <div>名：<input v-model="lastname" type="text"></div>
    
    <div>全名：{{name}}</div>
		
</template>


<script setup lang="ts">
let firstname = ref("张")
let lastname = ref("三")


let name =computed<string>({
   
})

```
:::


### 11.3、源码剖析
>[!tip]
> 1. 如果 computed 的第一个参数是函数，说明是`只读`的传入形式，将参数赋值给 getter, setter为空或在开发环境抛出警告。
> 2. 否则则为选项式传参，直接去第一个参数里面读 get 和 set 并赋值。

>[!warning]
> 1. `格式化参数`。
>    1. 如果 computed 的第一个参数是函数，说明是`只读`的传入形式，将参数赋值给 `getter`。setter为空或在开发环境抛出警告。
>    2. 如果computed的第一个参数是对象，说明是可读可写的形式。
> 2. 将get和set传给`ComputedRefImpl类`，默认声明了一个`value`和``dirty=true`（需要重新计算值）。
> 3. `get`中劫持了`value`,然后使用`toRaw`脱离`Proxy代理`，再判断`dirty`是否为true（脏值检测）
>    1. `dirty`为`true` 依赖改变，需要重新计算。
>    2. `dirty`为`false` 依赖不变，直接返回上一次的值。





## 十二、watch侦听器
>[!tip]
> 侦听特定的数据源，并在单独的回调函数中执行副作用。
> 接受三个参数
> 1. `监听源`
>    1. `ref`声明的响应式数据
>    2. `reactive`声明的响应式数据
>    3. 数据源组成的`数组`
>    4. `回调函数`（监听单个数据源）
> 2. 回调函数`Call Back(newValue,oldvalue)`
> 3. `options`配置项 （对象）
>    1. `immediate : true ` // 是否立即调用一次
>    2. `deep: true ` // 是否开启深度监听       `reavtive`声明的变量`默认开启深度监听`。

### 12.1、底层原理
>[!tip]
> 依赖收集和更新通知
> 1. 依赖收集：Vue会先找到被监听的数据属性所对应的`getter`，读取数据时会触发`getter`函数，在这个过程中，`watch`回调函数就会被收集为数据属性的一个依赖。
> 2. 依赖更新通知：当被监听的数据属性发生变化时，`Vue`会调用`setter`函数，通知所有依赖于这个数据属性的`watch`回调函数，并传入新值和旧值。
## 十三、watchEffect高级侦听器
>[!tip]
> 非惰性的，进入页面时函数自动调用一次。


## Vue3 传值

### 1. 父传子
>[!tip]
>- 父组件‌：使用`v-bind`或简写`:`将需要传递的数据绑定到子组件的props上。
>- 子组件‌：使用`defineProps`来定义props，接收父组件传递的数据。
::: details 点击查看代码
父组件中绑定数据
```html
<template>
  <div>
    <child :name="name"></child>
  </div>
</template>

<script setup lang="ts">
import { ref } from 'vue'

  const name = ref('张三')
</script>
```

子组件中定义props
```html
<template>
  <div>
    <p>姓名：{{name}}</p>
  </div>
</template>

<script setup lang="ts">
import { defineProps } from 'vue'

  const name = defineProps('name',{
    type:String,
    default:'张三'
  })
</script>
```
:::

### 2. 子传父
>[!tip]
>- 子组件‌：使用`defineEmits`定义可以触发的事件，并在需要时调用emit方法发送数据到父组件。
>- 父组件‌：监听子组件触发的事件，并处理接收到的数据。
::: details 点击查看代码
子组件中定义触发事件
```html
<template>
  <button @click="sendMessage">Send Message to Parent</button>
</template>

<script setup>
import { defineEmits } from 'vue';
const emit = defineEmits(['messageEvent']);
const sendMessage = () => {
  emit('messageEvent', 'Hello from Child');
}
</script>
```
父组件中监听事件并处理数据
```html
<template>
  <ChildComponent @messageEvent="handleMessage" />
</template>

<script setup>
import ChildComponent from './ChildComponent.vue';

const handleMessage = (message) => {
  console.log(message); // Output: Hello from Child
}
</script>

``` 
:::

### 3. 跨级组件传值
>[!tip]
>- 父组件‌：使用`provide`方法将需要传递的数据提供给`provide`属性，并在`inject`属性中注入需要的依赖。
>- 子组件‌：使用`inject`方法注入父组件提供的数据。
::: details 点击查看代码
父组件中提供数据
```html
<template>
  <div>
    <child :name="name" :age="age" />
  </div>
</template>

<script setup lang="ts">
import { ref, provide } from 'vue'
import Child from './Child.vue'

  const name = ref('张三')
  const age = ref(20)
  provide('info',{
    name,
    age
  })
</script>
```
子组件中注入数据
```html
<template>
  <div>
    <p>姓名：{{name}}</p>
    <p>年龄：{{age}}</p>
  </div>
</template>

<script setup lang="ts">
import { inject } from 'vue'

  const { name, age } = inject('info')
</script>
```
:::
